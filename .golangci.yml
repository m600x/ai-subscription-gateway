version: "2"

run:
  timeout: 3m

linters:
  default: standard
  exclusions:
    rules:
      # Test files intentionally ignore errors on httptest write/close helpers.
      - path: '(.+)_test\.go'
        linters:
          - errcheck
